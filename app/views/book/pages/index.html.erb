<p id="notice"><%= notice %></p>

<h1><%= book.title %>のページ一覧</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @book_pages.each do |book_page| %>
      <tr>
        <td class="with-image"><img width="200px" src="<%= book_page.image.url %>"></td>
        <!--td><%= link_to 'Show', book_page_path(book_id: book.id, id: book_page.id) %></td>
        <td><%= link_to 'Edit', edit_book_page_path(book_id: book.id, id: book_page.id) %></td-->
        <td class="with-button"><%= link_to '消す', book_page_path(book_id: book.id, id: book_page.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Page', new_book_page_path %>
